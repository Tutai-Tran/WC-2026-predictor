# Copy to .env and fill in. .env is gitignored.

# Free LLM key for the news-reading agent (Haiku). Get one at console.anthropic.com
ANTHROPIC_API_KEY=

# Free odds key (prediction signal + benchmark only, never betting).
# Get one at https://the-odds-api.com (free tier, no card).
THE_ODDS_API_KEY=
